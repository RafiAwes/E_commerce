import { NotFoundContent } from "@/components/shared/NotFoundContent";

/** Triggered by `notFound()` from a product, collection or occasion route. */
export default function StorefrontNotFound() {
  return <NotFoundContent />;
}
