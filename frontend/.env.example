# Canonical origin used by metadata, the sitemap and structured data.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# When the storefront is pointed at a real backend, the base URL for the
# API repositories lives here. Nothing reads it yet — the prototype is
# served entirely from src/data via the mock repositories.
# NEXT_PUBLIC_API_BASE_URL=https://api.aurelia.example.com
